# SAVEB 部署与数据迁移操作手册

整理日期：2026-09-14。复盘范围：2026-09-07 至 2026-09-13。

当前执行路线：**本地开发 → git push 到 GitHub → 服务器 git pull → 服务器构建 Docker 镜像 → 更新容器 → 健康和业务检查**。

本文依据上周服务器终端回执和当前三个仓库的 Dockerfile、Compose、初始化及迁移脚本整理。本次整理没有连接服务器执行发布。GitHub Actions + GHCR + runner 留作后续方案；当前运行不依赖它们。

## 1. 先选对流程

| 当前要做什么 | 阅读位置 | 是否重新导入旧业务数据 |
| --- | --- | --- |
| 已上线，只更新代码 | 第 4 节 | 否 |
| 只修改 .env / nginx.conf | 第 5 节 | 否 |
| 新增数据库字段或升级 Collector | 第 4.3、4.4 节 | 否，仅升级结构/调度元数据 |
| 新服务器首次部署 | 第 6 节，再按需执行第 7 节 | 仅首次迁移时执行 |
| 发布失败，需要回退 | 第 8 节 | 不自动回退数据库 |
| 旧系统又新增了数据 | 第 7.5 节 | 另做增量合并，不能重复全量导入 |

现在的服务器已经完成首次建表和业务导入，日常直接从第 3、4 节开始。命令中的 Bash 块在 Linux 服务器执行；PowerShell 块在本地 Windows 执行。不要把终端提示符、输出结果或 Markdown 反引号当成命令复制。

## 2. 上周完成了什么

### 2.1 最终部署状态

| 项目 | 已确认的结果 |
| --- | --- |
| 磁盘 | 根文件系统扩容后约 588 GB，可用约 507 GB；这是当时读数，当前空间用 df 再查 |
| 代码 | 三个新仓库均使用 CYKJ-2，已在服务器 /home/admin_chen/www 下 clone |
| 基础设施 | 新 PostgreSQL、API Redis、Collector Redis 已启动并健康 |
| API 数据库 | 先创建全部基础表和当前基础 RBAC，不依赖开发者本地数据库快照 |
| 旧数据 | 2026-09-11 已完成正式导入：38 张业务表、23,775 条记录；保留新 RBAC，附件已校验 |
| 迁移校验 | 演练保护 16 张表，检查 21 个业务序列；正式导入后恢复新服务 |
| 应用 | 新 API、Admin 已运行，后台首页已可访问；Collector 六个服务曾返回 Healthy |
| 采集凭据 | 排查过密码末尾全角字符丢失问题；修改环境变量后需重建全部 Collector 容器 |

容器 Healthy 不等于收单登录成功，也不等于业务数据全部验收完成。上周导入是一次快照，旧后台仍继续使用，不属于停机切换或持续同步。

9 月 14 日追加的自动采集修复属于本周变更：近期采集与历史单号复查拆分，旧积压任务通过 Collector 迁移 006 调整。服务器是否已经应用，以实际发布及任务结果为准，不能算入上周已完成事项。详见 [自动采集积压修复](../saveb-collector/自动采集积压修复.md)。

### 2.2 固定环境清单

| 项目 | 当前约定 |
| --- | --- |
| 服务器 / SSH | 192.168.11.84，22，admin_chen |
| 工作根目录 | /home/admin_chen/www |
| 三个仓库 | git@github.com:CYKJ-2/saveb-api.git；saveb-admin.git；saveb-collector.git |
| 新 Docker 网络 | saveb-production |
| 浏览器入口 | http://192.168.11.84:13000/dashboard/overview |
| Admin 宿主机监听 | 192.168.11.84:13000 → 容器 80 |
| API 宿主机监听 | 127.0.0.1:18088 → 容器 8080 |
| Collector 宿主机监听 | 127.0.0.1:18085 → 容器 8080 |
| Navicat | 192.168.11.84:5433，数据库 saveb，用户 saveb，密码取服务器私有配置 |
| 新数据库容器 | saveb-infra-postgres-1 |
| 新 PostgreSQL 卷 | saveb-production-postgres |
| 新附件卷 | saveb-production-attachments |
| API Laravel storage 卷 | saveb-api-production-storage |

容器互访使用网络别名及**容器端口**：Admin → `saveb-api-web:8080`；API → `saveb-collector-api:8080`；API/Collector → `saveb-api-postgres:5432`。浏览器不需要直接访问 API 或 Collector，也不需要 SSH 隧道。

旧项目 `saveb-erp-phase4-task2-a823095-20260718-190342`、其他 ERP 和禅道继续独立运行。本文命令只针对新项目，不修改旧卷、宿主机 SSH 配置或 Docker 全局配置；Docker 自身文件仍保存在其现有数据目录。

## 3. 每次发布前的准备

### 3.1 本地提交（PowerShell）

只提交有修改的仓库。以下以 API 为例，另外两个项目替换目录即可：

```powershell
Set-Location E:\wwwroot\saveb-api
git status --short
git diff --stat
git add .
git diff --cached --stat
git diff --cached --name-only
```

检查暂存清单没有真实 .env、私钥、数据库导出或业务备份，再执行：

```powershell
git commit -m "fix: describe this release"
git push origin main
```

修改代码后运行相关测试。Admin 可用 `npm.cmd run build`；API 使用对应模块 PHPUnit 配置；Collector 使用 pytest，数据库集成测试仅对独立测试库运行。不要在生产库执行测试或 migrate:fresh。

### 3.2 服务器检查及留存回退材料（Bash）

先在本地 PowerShell 登录服务器，后续 Bash 命令再在服务器执行：

```powershell
ssh admin_chen@192.168.11.84
```

发布前执行一次下列块，记录运行中的新应用镜像 ID、版本和私有配置。若是首次安装、相关容器或配置不存在，会跳过该项。

```bash
(
  set -eu
  umask 077
  RELEASE_BACKUP="/home/admin_chen/www/backups/release-$(date +%Y%m%d-%H%M%S)-$$"
  mkdir -p "$RELEASE_BACKUP"
  for app in saveb-api saveb-admin saveb-collector; do
    root="/home/admin_chen/www/$app"
    git -C "$root" status --short
    git -C "$root" rev-parse HEAD > "$RELEASE_BACKUP/$app.commit"
    for file in .env nginx.conf; do
      if [ -f "$root/$file" ]; then
        cp -- "$root/$file" "$RELEASE_BACKUP/$app.$file"
      fi
    done
  done
  for container in saveb-api-production-app-1 saveb-admin-production-web-1 saveb-collector-production-api-1; do
    if docker inspect "$container" >/dev/null 2>&1; then
      docker inspect --format '{{.Image}}' "$container" > "$RELEASE_BACKUP/$container.image-id"
      docker inspect --format '{{.Config.Image}}' "$container" > "$RELEASE_BACKUP/$container.image-tag"
    fi
  done
  df -h /home/admin_chen/www
  docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
  printf '本次回退材料：%s\n' "$RELEASE_BACKUP"
)
```

记下输出目录。目录含私有配置，仅留在服务器，不提交 Git。确认 git status 没有未解决冲突后发布；有 nginx.conf 等本地修改时先比较备份与新版本，合并后再继续，不使用 reset --hard 或自动丢弃配置。

### 3.3 数据库迁移前备份

仅代码更新且没有数据库修改时可按现有备份计划执行；涉及表结构或数据迁移时必须先备份。以下保存新库的一致性快照，旧库完全不操作：

```bash
(
  set -eu
  umask 077
  DB_BACKUP="/home/admin_chen/www/backups/database-$(date +%Y%m%d-%H%M%S)-$$"
  mkdir -p "$DB_BACKUP"
  docker exec saveb-infra-postgres-1 sh -c \
    'exec pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" -Fc' \
    > "$DB_BACKUP/saveb.dump"
  test -s "$DB_BACKUP/saveb.dump"
  docker exec -i saveb-infra-postgres-1 pg_restore --list \
    < "$DB_BACKUP/saveb.dump" > "$DB_BACKUP/archive-list.txt"
  (cd "$DB_BACKUP" && sha256sum saveb.dump > SHA256SUMS)
  printf '新库备份：%s\n' "$DB_BACKUP"
)
```

archive-list 只是备份目录可读检查，不等于完整恢复演练，也不包含附件。涉及文件格式或附件变更时，需要同时备份附件卷；首次业务迁移脚本已自动包含新库和附件备份。完整恢复验证参考 [采集巡检与备份说明](../saveb-collector/采集巡检与备份说明.md)。

## 4. 日常代码更新：按需执行一个项目

以下块使用子 Shell，任一步失败就停止该块，不会继续切换到未构建成功的镜像。每个项目自行设置 RELEASE_IMAGE，不能沿用另一个项目的变量。默认部署 main，发布期间不要同时运行另一套手动或自动发布。

### 4.1 只更新 Admin

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-admin
  git pull --ff-only origin main
  export RELEASE_IMAGE="saveb-admin:server-$(git rev-parse HEAD)"
  docker build \
    --build-arg NODE_IMAGE=public.ecr.aws/docker/library/node:22-bookworm-slim \
    --build-arg NGINX_IMAGE=public.ecr.aws/docker/library/nginx:stable-alpine \
    -t "$RELEASE_IMAGE" .
  docker compose config --quiet
  docker compose up -d --no-build --pull never --wait
  curl -f http://192.168.11.84:13000/healthz
)
```

刷新浏览器并验证修改的页面。生产页面来自镜像中的 dist，git pull 后只执行 restart 不会更新前端代码。

### 4.2 更新 API（没有新增数据库迁移）

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-api
  git pull --ff-only origin main
  export RELEASE_IMAGE="saveb-api:server-$(git rev-parse HEAD)"
  docker build --target production \
    --build-arg PHP_IMAGE=public.ecr.aws/docker/library/php:8.3-fpm-bookworm \
    --build-arg COMPOSER_IMAGE=public.ecr.aws/docker/library/composer:2 \
    -t "$RELEASE_IMAGE" .
  docker compose -f docker-compose.server.yml config --quiet
  docker compose -f docker-compose.server.yml up -d --no-build --pull never --wait app web worker
  curl -f http://127.0.0.1:18088/up
)
```

app、web、worker 必须同时更新。纯 PHP 代码更新不需要重建 PostgreSQL、Redis，也不执行首次初始化或旧数据导入。

### 4.3 API 有新增数据库迁移

先 pull 和 build，使用上节相同命令，但先不执行最后的 up。确认版本兼容和维护窗口；暂停新后台写入及所有直连新库的人工操作。需要协调升级 Collector 的版本时，先准备好其镜像。

保存当前运行的 Collector 容器 ID，只暂停这些新容器；暂停 API。以下命令在同一个终端执行，暂停后若出错不要盲目启动不兼容的旧服务：

```bash
cd /home/admin_chen/www/saveb-api
export RELEASE_IMAGE="saveb-api:server-$(git rev-parse HEAD)"
COLLECTOR_RUNNING=$(docker ps -q --filter label=com.docker.compose.project=saveb-collector-production)
(
  set -eu
  for container in $COLLECTOR_RUNNING; do
    docker stop --time 300 "$container"
  done
  docker compose -f docker-compose.server.yml stop web worker app
)
```

确认上述服务停止，再执行第 3.3 节数据库备份。备份成功后执行：

```bash
docker compose -f docker-compose.server.yml --profile tools run \
  --rm --no-deps --pull never migrate php artisan migrate --force &&
docker compose -f docker-compose.server.yml up -d --no-build --pull never --wait app web worker &&
curl -f http://127.0.0.1:18088/up
```

API 健康且原 Collector 与新结构兼容时，恢复之前运行的 Collector：

```bash
for container in $COLLECTOR_RUNNING; do
  docker start "$container" || break
done
```

若 Collector 也有对应升级，改执行 4.4 的新版迁移和启动，不启动旧版本。随后检查 Collector 健康和任务结果。`migrate --force` 只执行待执行的 Laravel 迁移；不是重建数据库，也不会因为 RbacSeeder.php 改了就自动重跑种子。已有系统权限变更应提供明确增量迁移，不靠重新初始化覆盖账号授权。

### 4.4 更新 Collector（包含本次自动采集修复）

先按第 3.3 节备份；随后执行。此流程适用于已经完成业务和规则准备、六个 Collector 服务均应启用的环境：

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-collector
  git pull --ff-only origin main
  export RELEASE_IMAGE="saveb-collector:server-$(git rev-parse HEAD)"
  docker build \
    --build-arg PYTHON_IMAGE=public.ecr.aws/docker/library/python:3.12-slim-bookworm \
    -t "$RELEASE_IMAGE" .
  docker compose -f docker-compose.server.yml config --quiet
  docker compose -f docker-compose.server.yml stop beat worker history maintenance logistics api
  docker compose -f docker-compose.server.yml --profile tools run \
    --rm --no-deps --pull never migrate
  docker compose -f docker-compose.server.yml up \
    -d --no-build --pull never --wait api worker history maintenance logistics beat
  curl -f http://127.0.0.1:18085/ready
)
```

migrate 服务依次执行 `scripts/migrate.py` 和 `scripts/preflight.py`。迁移 006 会把旧的混合 refresh 积压任务转为历史任务，保留进度和订单；需要先停止旧 worker/Beat。本次新版 /ready 要求 Collector schema version 6。

自动采集默认间隔 30 分钟，在 **系统管理 → 采集管理** 修改。新版默认近期任务是“当天＋昨天”，共两片、先今天；历史 Pending 发现和历史订单复查独立执行。自定义范围是一次任务，不会创建重复定时采集。账户锁仍可能让即时任务等待当前历史分片结束。

## 5. 只改服务器配置时怎么生效

先取正在运行的镜像，避免 .env 修改被误认为要重建源码。三个项目分别执行，不能混用 RELEASE_IMAGE。

| 修改内容 | 生效方式 |
| --- | --- |
| Admin / API / Collector 源码、依赖、Dockerfile | 第 4 节重新 build 后 up |
| .env 中服务参数、账号密码、端口 | 使用当前镜像 up --force-recreate |
| 根目录 nginx.conf | nginx -t 通过后 restart web |
| 采集管理页面中的自动间隔 | 保存即生效，无需重启 |
| 仅 README / 文档 / 宿主机迁移工具 | git pull 即可；容器运行的程序仍需要重建镜像 |

以 Collector 修改收单密码为例：

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-collector
  export RELEASE_IMAGE=$(docker inspect --format '{{.Image}}' saveb-collector-production-api-1)
  docker compose -f docker-compose.server.yml up \
    -d --no-build --pull never --force-recreate --wait api worker history maintenance logistics beat
  curl -f http://127.0.0.1:18085/ready
)
```

API .env 更新使用同样方式读取 `saveb-api-production-app-1` 镜像并重建 `app web worker`；Admin 读取 `saveb-admin-production-web-1` 镜像并用根目录 Compose 重建 `web`。不要只 restart；容器环境变量在创建时注入。

Nginx 检查及应用（分别选一个项目）：

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-admin
  export RELEASE_IMAGE=$(docker inspect --format '{{.Image}}' saveb-admin-production-web-1)
  docker compose exec -T web nginx -t
  docker compose restart web
  curl -f http://192.168.11.84:13000/healthz
)
```

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-api
  export RELEASE_IMAGE=$(docker inspect --format '{{.Image}}' saveb-api-production-app-1)
  docker compose -f docker-compose.server.yml exec -T web nginx -t
  docker compose -f docker-compose.server.yml restart web
  curl -f http://127.0.0.1:18088/up
)
```

Collector 没有 nginx.conf。数据库/Redis 密码不是普通应用参数：已有 PostgreSQL 卷不会因修改 POSTGRES_PASSWORD 自动修改库内用户密码；此类修改需要单独协调服务端密码和所有客户端。

## 6. 全新服务器首次部署（当前服务器不要重做）

顺序：clone → 私有配置 → 基础设施 → API 镜像/首次初始化 → API/Admin → Collector 建表及接口 → 可选旧数据迁移 → Collector 预检/手动采集 → Beat。

### 6.1 拉代码及填写配置

已有仓库目录只 pull，不重复 clone。SSH 公钥应属于有仓库访问权限的 GitHub 账号；服务器已能拉取时无需重配。GitHub 出站认证与服务器 authorized_keys 是两回事，不改原 authorized_keys。

```bash
mkdir -p /home/admin_chen/www
cd /home/admin_chen/www
git clone git@github.com:CYKJ-2/saveb-api.git
git clone git@github.com:CYKJ-2/saveb-admin.git
git clone git@github.com:CYKJ-2/saveb-collector.git
```

每个仓库仅在 .env 不存在时复制模板，保留现有配置：

```bash
(
  set -eu
  umask 077
  for app in saveb-api saveb-admin saveb-collector; do
    root="/home/admin_chen/www/$app"
    if [ ! -e "$root/.env" ] && [ ! -L "$root/.env" ]; then
      cp "$root/.env.example" "$root/.env"
    fi
  done
  mkdir -p /home/admin_chen/www/saveb-collector/config
)
```

只填写真实私有 .env，口令不放在本文。上周入口已改为内网共享访问，模板里如仍有回环/SSH 隧道注释，以以下实际值为准：

| 配置文件 | 必须核对 |
| --- | --- |
| saveb-api/.env | APP_ENV=production；APP_DEBUG=false；API_PORT=18088；APP_URL、FRONTEND_URL、CORS_ALLOWED_ORIGINS 均为 http://192.168.11.84:13000 |
| saveb-api/.env | DB_HOST=saveb-api-postgres；DB_PORT=5432；DB_DATABASE=saveb；DB_USERNAME=saveb；填写 DB_PASSWORD、REDIS_PASSWORD、APP_KEY |
| saveb-api/.env | REDIS_HOST=saveb-api-redis；POSTGRES_BIND_IP=192.168.11.84；POSTGRES_HOST_PORT=5433 |
| saveb-api/.env | SAVEB_COLLECTOR_URL=http://saveb-collector-api:8080；SAVEB_COLLECTOR_ACCOUNT=default；填写共享 SAVEB_COLLECTOR_TOKEN |
| saveb-admin/.env | ADMIN_BIND_IP=192.168.11.84；ADMIN_PORT=13000；DEPLOY_NETWORK=saveb-production |
| saveb-collector/.env | SAVEB_APP_ENV=production；COLLECTOR_PORT=18085；SAVEB_PUBLISH_API=true；SAVEB_SOURCE_ACCOUNT=default |
| saveb-collector/.env | SAVEB_DATABASE_URL 指向同一个 saveb 库、saveb-api-postgres:5432；DSN 密码特殊字符要 URL 编码 |
| saveb-collector/.env | SAVEB_API_TOKEN 与 API 共享 Token 相同；三个 Redis URL 使用 saveb-collector-redis:6379 的 /0、/1、/2 |
| saveb-collector/.env | 填 SAVEB_DH_USERNAME、SAVEB_DH_PASSWORD，保留密码全角/半角字符；账号密码方式下 SAVEB_DH_COOKIE 留空 |
| saveb-collector/.env | 物流 Key 未配置时 SAVEB_LOGISTICS_ENABLED=false；规则/汇率来自业务库或显式私有文件 |
| 三个 .env | DEPLOY_NETWORK=saveb-production |

Admin Nginx 保留 listen 80 和上游 saveb-api-web:8080；API Nginx 保留 listen 8080 和 app:9000。端口/域名主要通过 .env 控制，不在 Compose 里写死服务器 IP。

### 6.2 启动新基础设施

Docker Hub 曾超时，当前使用已成功拉取过的 ECR Public 基础镜像。saveb-api/.env 填：

```dotenv
INFRA_POSTGRES_IMAGE=public.ecr.aws/docker/library/postgres:16-alpine
INFRA_REDIS_IMAGE=public.ecr.aws/docker/library/redis:7-alpine
```

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-api
  docker compose -f docker-compose.infra.yml config --quiet
  docker compose -f docker-compose.infra.yml pull
  docker compose -f docker-compose.infra.yml up -d --wait --pull never
  docker compose -f docker-compose.infra.yml ps
)
```

应为 postgres、redis、collector-redis 三个服务 Healthy。基础设施一般只创建一次，普通代码发布不重复重建。换镜像仓库只解决对应镜像拉取问题；服务器仍需访问 apt、Composer、npm、pip 等依赖源。

### 6.3 构建 API 并初始化空库

先执行第 4.2 节的 pull、export RELEASE_IMAGE 和 docker build，暂不执行 up。全新 APP_KEY 可在已构建镜像中生成，将结果手动填入 API .env；已有系统保持原值：

```bash
cd /home/admin_chen/www/saveb-api
export RELEASE_IMAGE="saveb-api:server-$(git rev-parse HEAD)"
docker run --rm --network none --entrypoint php "$RELEASE_IMAGE" \
  -r 'echo "base64:".base64_encode(random_bytes(32)).PHP_EOL;'
```

仅对全新空库执行：

```bash
docker compose -f docker-compose.server.yml --profile tools run \
  --rm --no-deps --pull never migrate php artisan server:database-init --force &&
docker compose -f docker-compose.server.yml up -d --no-build --pull never --wait app web worker &&
curl -f http://127.0.0.1:18088/up
```

该命令从仓库基线创建结构、执行 Laravel 增量迁移并建立基础 RBAC。默认管理员 super_admin，初始密码 123456；已有账号使用现有密码。当前线上已初始化和导入，**不再运行 server:database-init**。

之后按第 4.1 节构建并启动 Admin，验证登录和权限。业务表为空时没有订单是正常现象。

### 6.4 Collector 首次只建表、启动接口

先用第 4.4 节的 pull、export RELEASE_IMAGE、docker build 构建镜像，暂不执行 stop/migrate/up 全套。随后：

```bash
cd /home/admin_chen/www/saveb-collector
export RELEASE_IMAGE="saveb-collector:server-$(git rev-parse HEAD)"
docker compose -f docker-compose.server.yml --profile tools run \
  --rm --no-deps --pull never migrate python scripts/migrate.py &&
docker compose -f docker-compose.server.yml up -d --no-build --pull never --wait api &&
curl -f http://127.0.0.1:18085/ready
```

需要迁入旧业务时，先执行第 7 节；首次迁移前不要开启采集写入新业务表。没有旧数据的新安装仍需提供真实站点规则及汇率，不能用空规则绕过 preflight。

准备好数据、规则和收单凭据后：

```bash
cd /home/admin_chen/www/saveb-collector
export RELEASE_IMAGE="saveb-collector:server-$(git rev-parse HEAD)"
docker compose -f docker-compose.server.yml --profile tools run \
  --rm --no-deps --pull never migrate python scripts/preflight.py &&
docker compose -f docker-compose.server.yml up \
  -d --no-build --pull never --wait api worker history maintenance logistics
```

从首页手动“采集当天数据”，确认任务成功、订单可见后才开启定时：

```bash
docker compose -f docker-compose.server.yml up -d --no-build --pull never beat
```

同一环境只运行一个 Beat。首次 preflight 成功只验证数据库、Redis、规则等依赖，不代表收单凭据已经完成真实登录验收。

## 7. 旧业务数据迁移：上周已完成，保留为复用流程

### 7.1 来源、目标及保护范围

| 项目 | 本次值 |
| --- | --- |
| 来源库 | phase4 |
| 来源容器 | saveb-erp-phase4-task2-a823095-20260718-190342-postgres-1 |
| 来源 API | saveb-erp-phase4-task2-a823095-20260718-190342-api-1 |
| 目标库 / 容器 | saveb / saveb-infra-postgres-1 |
| 旧附件卷 | saveb-erp-phase4-task2-a823095-20260718-190342_attachments |
| 新附件卷 | saveb-production-attachments |

迁入订单、规则、汇率、统计、发票、PayPal、采购、仓库、附件等 38 张业务表；保留目标 users、roles、permissions、role_permissions、user_roles、api_tokens、audit_logs、migrations 及其他未参与导入的表。不迁入旧 Knex 迁移记录，不直接替换整库。

为避免列顺序和字段差异导致失败，脚本按明确列名 COPY；新增字段使用目标允许的默认值/NULL，实际必填缺失、唯一冲突、CHECK 和业务外键通过快照演练验证。业务序列单独校验并调整，避免新增记录撞主键。

旧用户 UUID 保留，本次三条历史用户外键保持 NOT VALID：background_jobs_creator_fk、order_user_overrides_updated_by_user_fk、workflow_events_actor_fk。NOT VALID 不等于删除约束，新写入仍受约束；不把全部历史操作者改成新管理员。

### 7.2 只读预检

**以下三阶段用于新的首次迁移目标。当前线上业务表已非空，不要重跑正式导入。** 脚本在 Linux 宿主机执行，不需要重新构建 API 镜像。

```bash
cd /home/admin_chen/www/saveb-api
git pull --ff-only origin main &&
python3 scripts/legacy_import_preflight.py \
  --source-container saveb-erp-phase4-task2-a823095-20260718-190342-postgres-1 \
  --target-container saveb-infra-postgres-1 \
  --output "/home/admin_chen/www/backups/legacy-preflight-$(date +%Y%m%d-%H%M%S)-$$"
```

上周结果：38 张业务表、阻断项 0、目标新增字段 81、历史用户外键待核对 14。预检无阻断仍不等于可直接导入，继续演练。

### 7.3 一致性快照和隔离库演练

```bash
cd /home/admin_chen/www/saveb-api
python3 scripts/legacy_import_rehearsal.py \
  --source-container saveb-erp-phase4-task2-a823095-20260718-190342-postgres-1 \
  --target-container saveb-infra-postgres-1 \
  --output "/home/admin_chen/www/backups/legacy-rehearsal-$(date +%Y%m%d-%H%M%S)-$$"
```

导出旧业务快照、备份新库，恢复到两个隔离临时库，试导入并校验内容、保护表、约束和序列。这个阶段不写正式业务库，也不复制附件。

上周通过结果及材料：

```text
业务表：38；记录：23775；受保护表：16；业务序列：21
材料：/home/admin_chen/www/backups/legacy-rehearsal-20260911-110739-90176
临时源库：saveb_rehearsal_src_227550333e91
临时目标库：saveb_rehearsal_trial_227550333e91
```

保留本次演练材料和临时源库，正式阶段要使用同一快照。复用流程时记录新输出路径，不把上周路径当作新一次迁移的材料。

### 7.4 正式导入和附件复制

首次新目标演练通过后，指定刚才通过的材料目录。下面占位路径必须替换，不能直接执行：

```bash
SNAPSHOT_DIR='/home/admin_chen/www/backups/替换为本次通过的legacy-rehearsal目录'
cd /home/admin_chen/www/saveb-api
python3 scripts/legacy_import_apply.py \
  --snapshot "$SNAPSHOT_DIR" \
  --output "/home/admin_chen/www/backups/legacy-check-$(date +%Y%m%d-%H%M%S)-$$"
```

这是只检查模式。确认目标仍为空业务库、检查通过并进入新系统维护窗口后，执行一次正式写入：

```bash
python3 scripts/legacy_import_apply.py \
  --snapshot "$SNAPSHOT_DIR" \
  --output "/home/admin_chen/www/backups/legacy-apply-$(date +%Y%m%d-%H%M%S)-$$" \
  --apply
```

脚本核对快照/附件，暂停**新 API 和 Collector**，备份目标数据库及新附件卷；按 SHA256 复制附件，同路径冲突不覆盖；单事务导入和校验，最后恢复原先运行的新服务并检查健康。旧后台保持运行。未运行的 Collector 不会因此被顺带开启。

上周正式执行已返回“正式导入完成：38 张业务表；RBAC 保留；附件已校验；新服务已恢复”，材料目录：

```text
/home/admin_chen/www/backups/legacy-apply-20260911-112355-90176
```

失败时先读 apply-state.json / failure.json。database_committed=true 表示数据库已提交，即使后续健康检查失败也不能再次导入；提交状态不确定时先核实。附件复制不属于数据库事务，不能假定 SQL 回滚会自动撤销已复制文件。完整处理见 [旧业务迁移说明](LEGACY-BUSINESS-IMPORT.md)。

### 7.5 以后旧系统继续产生的数据怎么处理

此次是快照导入，快照之后的旧系统变更不保证包含在新库。当前全量脚本要求目标候选业务表为空，不能用于已经上线的新库增量同步。

收单订单可用 Collector 的自定义“范围更新”重新抓取，按单号新增或更新并保留人工调整、人工删除；只执行一次。它不负责同步旧 ERP 的采购、仓库、发票、PayPal、人工操作等变更。上述数据需要另行制定按主键/时间和字段映射的增量合并及冲突核对方案，不能靠清空新库重导解决。

## 8. 回退、验收与常见问题

### 8.1 回退应用镜像

使用第 3.2 节保存的准确镜像 ID，不用“当前 git HEAD”猜旧版本。下面以 Admin 为例，先替换回退材料路径：

```bash
(
  set -eu
  cd /home/admin_chen/www/saveb-admin
  RELEASE_BACKUP='/home/admin_chen/www/backups/替换为发布前记录目录'
  export RELEASE_IMAGE=$(cat "$RELEASE_BACKUP/saveb-admin-production-web-1.image-id")
  docker image inspect "$RELEASE_IMAGE" >/dev/null
  docker compose up -d --no-build --pull never --wait
  curl -f http://192.168.11.84:13000/healthz
)
```

API 使用 app 的 image-id 和 server Compose 更新 app/web/worker；Collector 使用 api 的 image-id 和 server Compose 更新六个服务。先检查当前 .env、挂载 nginx.conf、Compose 和数据库结构与旧镜像是否兼容，必要时逐项对照私有备份恢复配置。

镜像回退不会回退数据库、已导入订单或 Collector 元数据。特别是迁移 006 后直接回退到要求 schema version 5 的旧 Collector 会健康失败，不能照搬镜像回退命令；应提供兼容修复版本，或另行制定停写、备份及恢复方案。不要自动执行 migrate:rollback 或恢复旧全库覆盖新产生的数据。

### 8.2 发布后的最小验收

```bash
curl -f http://127.0.0.1:18088/up
curl -f http://127.0.0.1:18085/ready
curl -f http://192.168.11.84:13000/healthz
docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
```

再在浏览器完成：登录；检查账号角色权限；选择确实有历史数据的日期查看首页和订单；抽查采购/仓库/发票及附件；手动采集当天并观察最终状态；等下一次自动计划核对新任务。健康接口不能代替这些业务检查。

查看对应服务日志，不输出整份 .env 或 docker inspect 环境变量：

```bash
docker logs --tail 100 saveb-api-production-app-1
docker logs --tail 100 saveb-api-production-worker-1
docker logs --tail 100 saveb-collector-production-worker-1
docker logs --tail 100 saveb-collector-production-beat-1
```

| 遇到的问题 | 处理方式 |
| --- | --- |
| git pull 提示本地文件冲突 | 先保留私有配置、比较并解决冲突；不要直接覆盖 nginx.conf 或 Compose |
| docker-compose.yml 显示 symlink，内容变成链接目标 | 仓库文件模式应为 100644；从本地修复后提交并 pull，git ls-files -s 核对，不能只改文件文字 |
| Docker Hub 拉取超时 | 使用上述 ECR 构建参数和 infra 镜像变量，不改全局 Docker 配置 |
| GHCR unauthorized | 当前不使用 GHCR；RELEASE_IMAGE 填服务器构建的 saveb-项目:server-SHA |
| compose run 提示 unknown flag --no-build | run 不加 --no-build；当前生产 Compose 无 build，先显式 docker build，再 run --pull never |
| 容器健康但采集 AUTH_CREDENTIALS_REJECTED | 核对收单账号、密码字符和容器实际配置，重建全部 Collector；Healthy 不是来源登录验证 |
| 自动任务显示几千分片、跨天未采集 | 更新 Collector 和 Admin，执行迁移 006；不要清库或清队列 |
| 首页无数据但迁移成功 | 核对 API 实际数据库、前端代理、所选日期/筛选条件、接口错误；不要直接重复导入 |
| 同事打不开后台 | 确认 Admin 绑定 192.168.11.84:13000，客户端能访问内网；不使用只适合单人访问的隧道 |
| Actions 仍构建 | 仓库变量 DEPLOY_ENABLED=false 仅控制工作流部署条件，不会关闭测试/构建；暂不使用时可在 GitHub 禁用工作流 |

### 8.3 后续怎样更快

1. 三个仓库保持独立；只更新有代码变化的项目，跨项目兼容变更按 API → Collector → Admin 顺序安排。
2. 保留服务器 .env、统一 nginx.conf 和端口变量；不在服务器反复修改 Compose，不重复 clone 或配置 SSH。
3. 保留可用基础镜像和构建缓存，默认不加 --no-cache；镜像构建成功后才切换容器。
4. 基础设施、首次初始化、旧业务全量迁移各做一次，日常直接使用第 4 节。
5. 每次记录提交 SHA、镜像 ID和备份目录；保留已验证版本，回退时不重新构建或临时找旧代码。
6. 暂不引入新的发布平台；现有流程稳定后，再根据网络和费用需求决定是否启用 Actions/GHCR。云端测试、镜像构建和服务器实际运行是三个不同阶段。

不要执行全局 docker system prune --volumes、全局容器停止、生产 down -v、清表、migrate:fresh 或删除旧项目目录来“加速”。这些操作不是本流程的部署步骤。
